{
  "name": "spoonasia",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "npm run build && npx serve dist -l ${PORT:-8080}",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.16.18",
    "serve": "^14.2.4"
  }
}